/**
 * @deprecated Import from `@/core/config/DiagramConfig`.
 * This compatibility boundary keeps existing component consumers working while
 * the configuration implementation remains in the inward core config layer.
 */
export * from '../../config/DiagramConfig';
