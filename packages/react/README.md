# @vizly/react

Open React integration boundary for the compiled Vizly editor runtime. Hosts
provide their own configured i18next instance and receive only portable
contracts through the public ref and event APIs.
