# 布局与连线专项验收检查点

**通用运行时补充（2026-09-05，本轮工作区）**：已接入有限局部网格寻路候选，并修复首个交叉组独占候选的问题。大交叉组仍保留原有端口联合搜索预算，但允许最多四条候选边尝试局部修复；其他节点和连线继续参与全图检查。不含图ID特判，不扩大搜索深度、评估次数或超时。企业架构记录终态的局部回放约1.36秒，严格交叉25→23，回折惩罚120不变、穿节点0不变、净距违规仍7条；总折点90→93、总长度67545→67741。此记录不等于当前浏览器/完整Worker成功，也不证明整体路径已达到门禁。证据 `enterprise-maze-coverage-result.json`、`mazeCoverageProbe.test.ts`；对照 `enterprise-segment-beam-result.json`。下一步仍需最终构建矩阵、复杂图收敛与明确失败原因。


最新检查点：TMS 复合 BT 外绕已修复。源端口候选过去只搜索源与旧路径之间的通道，漏掉换端口后更短的外侧通道。新增基于业务节点整体边界的有限候选，保留目标共享后缀，沿用原 32/每边8 次评估与全部门禁。真实失败边从8折降为3折，图总长度16910→16601；原序列泳道BT→复合BT→泳道RL→全图LR全部通过。证据 tms-exterior-browser.log、tms-exterior-domain-compound-elk-bt.json/png；截图为32%概览。相关36项回归、verify:static、TS6、CI覆盖1100文件通过，预编译几何不变、仅源码身份更新。此前未解决TMS的记录保留为历史；当前剩余合成视觉案例、最终构建全矩阵及完整CI/报告，不声称I5已完成。

2026-09-05；源码基点 `81ba1cf3`。这是证据索引和剩余任务，不是最终通过声明。历史浏览器记录来自其各自构建，不能外推为当前构建的全量矩阵通过。工作区其他修改保留；本地提交未推送。

证据目录：`C:/Users/juhon/.codex/visualizations/2026/09/05/01a06fb0-6d82-76a1-a1a9-5e6522633b0c/implementation/`。下表文件名均相对于该目录。

| 用户验收项 | 已有证据 | 尚未证明的范围 |
| --- | --- | --- |
| 布局 1：菜单、图标、方向与业务流 | 菜单测试、方向策略测试，四代表图历史方向矩阵 | 最终构建逐项菜单/图标视觉复核 |
| 布局 2：四方向、跨引擎、快速切换 | 历史 16 泳道方向案例；需求分配/物流/WMS 跨引擎记录；真实鼠标忙碌期禁用及完成后切换通过 | TMS 原跨引擎失败已修复，见文首记录；最终构建矩阵尚未重跑，快速操作证据限需求分配场景 |
| 布局 3：失败保留画布且状态一致 | `useLayoutRoutingTransaction.test.tsx` 覆盖当前失败回滚、过期 epoch、延迟 ELK 结果与发布顺序 | Hook 测试不等于浏览器快速操作与提示一致性验收 |
| 布局 4：同组等宽/等高 | `display-routing-semantic-audit.mjs` 扫描实际容器 DOM；新跨引擎记录启用检查 | 旧 16 方向记录不能补记为已执行新尺寸审计 |
| 布局 5：长度与空白 | global/compact/auto 实现及策略测试；已修复无关子域伪串行 | 必要主链长度、算法冗余、需分段视图的逐图定量解释尚未收口 |
| 布局 6：并行、循环、跨域、嵌套、折叠、不均衡 | 布局策略测试；`i5-topology-edit-cycle.json` 的 10 项编辑操作；空图/单节点/12:1 嵌套无边四方向测试 | 已有五类合成图 × TB/LR 共10项补充记录；最终构建复核及长标题裁切问题仍待处理 |
| 布局 7：保存、重载、页面复制与隔离 | `i5-multipage-production.json`：三页、布局隔离、标签文本 marker 与手动偏移；脚本显式排除另一页 marker | 当前最终构建复核待最终矩阵；此记录在概览 LOD 下不证明标签可读 |
| 连线 1–3：端点、绕路、共享主干 | 多处捕获回归；最新 `demand-paired-browser.log`；硬质量与实际 SVG 检查 | TMS 原8折问题已修复至3折；企业架构图仍有交叉与净距违规，不能用局部改善代替最终通过 |
| 连线 4–5：最终路径上的标签与分支归属 | `wms-halfzoom-fixed.json`、最小短分支回归、拓扑编辑矩阵 | 每次编辑后密集标签的归属与箭头遮挡仍需按合成案例复核 |
| 连线 6：报告、最终数据、屏幕一致 | 浏览器矩阵检查实际路径、节点几何、渲染 authority 与 attachment | 未经同一构建执行的历史记录不得拼成最终全通过 |
| 连线 7：缩短、修端点与最终稳定化 | 已修复需求分配端点回退、商业净距双边依赖；精确最终报告保留 | TMS 原回绕已定位并修复；复杂图的多阶段修复收敛、长耗时仍需验证 |
| 连线 8：缩放可读性 | `i5-label-zoom.json`，四代表图 × 50/100/150%，12 项 error 均为 null | 范围为初始场景的已挂载标签，不保证全部同时位于视口或任意缩放比例 |
| 通用性：极端、长文本、密集标签 | 既有策略测试与缩放反例，原图 ID 被换为普通 ID 的小型回归 | 最终报告需列出闭合器现有 48 边/128 节点适用限制，不能声称任意规模支持 |
| 工程门禁 | 最新批次 `verify:static`、TS6、CI 收录 1100 文件及定向回归通过 | 历史完整 `verify` 有 PropertyPanel 超时；当前静态门禁、TS6和544项共享连线回归通过，统一CI已结束：46/49分片通过；WMS净距、物流路径点数、策略加载复用5秒超时导致3分片失败，不记为全绿 |

## 官方资料对照

合成浏览器补充（825d3348）：单节点、12:1 嵌套孤立节点、并行汇合、跨域反馈、长条件标签五类 × TB/LR 共10项，拓扑保留和实际泳道尺寸断言通过；实际标签遮挡、渲染缺失、拒绝和低对比计数均为0。证据 `i5-synthetic-browser.json`、`synthetic-*.png`。数据通过已挂载的隔离画布接口注入，再以菜单执行布局，不计作导入功能验收。100%截图只覆盖部分视口；长节点标题有裁切，孤立节点案例仍有明显交叉轴长度/空白，因此不把“无标签遮挡”写成“所有文本与空间利用均已理想”。最初空白画布和自定义URL加载探针未能装载数据，属于未成功建立的验收环境，不据此推定布局失败。

快速激活补充（当前生产构建）：`i5-rapid-switch.json/log` 记录复合 BT→泳道 RL→泳道 TB，点击间隔377/417ms，三次激活时均 busy。最后任务4 committed，菜单选中 TB 的断言通过，最终路径签名和节点几何在相隔3秒的观察中一致。此证据使用浏览器 DOM 菜单激活；物理鼠标版本在第二次展开时找不到菜单项，尚未完成。等待器曾把旧任务3的 cancelled 状态当作失败提前退出；补充观察证明最终任务成功，不能据此报告产品取消缺陷。

真实鼠标补充：`ModernFlowchartToolbar.tsx` 的 Dropdown 与触发按钮均在 layoutBusy 时 disabled；既有 mobile 测试也断言禁用期间不产生布局调用。此前鼠标找不到第二菜单属于这个预期契约，DOM 直接激活隐藏菜单项绕过了正常入口。`i5-rapid-pointer.json/png`、`i5-rapid-pointer-guard.log` 证明：任务2期间触发按钮 disabled/aria-busy 均为 true，连续三次鼠标点击后任务号仍为2；完成后通过鼠标选择泳道 TB，任务3 committed，选中项和稳定几何一致，无错误提示。截图为33%概览，不额外证明标签可读性。本项无需修改生产代码。

TMS 端口补充：保留目标共享后缀、改源右端口的候选可降至5折点、长度4640.5（原4908），净距合格，但与 GPS→执行、执行→承运商两条连线发生严格交叉。见 `tms-source-port-probe.json`。该候选不能提交；未修改生产路由或质量预算。

- [yFiles Edge Routing](https://docs.yworks.com/yfiles-html/dguide/polyline_router/) 分别建模节点净距、边间距、端段长度、单调性和局部更新范围。这支持将净距与折点作为不同维度检查，也支持只修改有问题的路径。该工具的“preferred”距离不是本项目 48px 硬门禁的放宽依据。
- [ELK Layered](https://eclipse.dev/elk/reference/algorithms/org-eclipse-elk-layered.html) 描述层次方向、正交端口约束及跨层级连接支持。这适用于比较布局与端口语义；“尽量同向”不代表反馈环可以反转业务边，也不保证域内紧凑和全局阶段顺序能同时最优。
- 同组泳道等宽/等高来自本项目明确要求，不是上述资料推导出的行业通则。是否采用更紧凑的视觉风格属于偏好；不能覆盖业务边语义、净距和泳道尺寸约束。

## 剩余批次及成本控制

TMS 几何诊断补充：原路径左侧长通道若统一取 x=267，会穿过 `frp`（x=128–300）；其右侧商业净距要求 x≥348，而上方 `logistics-oms` 左侧要求 x≤347。右侧末段也不能简单拉直到目标 x=1626：`fleet-management` 左边界1665只留下39px；附近 `driver-management` 右边界1545与48px净距共同限定直通走廊为1593≤x≤1617。这解释了为何独立删掉两端狗腿均不可接受，不代表所有端口或布局候选无解。下一步应验证端口/局部布局候选，而不是继续尝试已被节点阻断的单通道平移；联动其他边本身不能消除这些节点障碍。原始数据 `tmp/tms-compound-bt-request.json`、`tmp/tms-compound-bt-response.json`；8 折点路径与试验记录见 `current-handoff.md`。

1. 必须修复：TMS 跨引擎多折点。先限定几何反例，找到候选后补最小回归与原序列验证；不能增加允许折点或长度预算。工作量待候选验证后估计，当前不承诺无依据工期。
2. 必须验证：浏览器快速切换与合成案例；补缺失场景而非重写现有多页/缩放测试。预计 1 个验证批次，若发现产品缺陷另拆修复提交。
3. 值得优化：对剩余长泳道报告关键路径长度、冗余间距和跨域回移，区分必要长度与算法空白。以四代表图作比较；不做单泳道裁切。
4. 收口：最终构建代表矩阵与完整门禁、按严重度的问题报告及证据索引。历史结果保留，不用复跑成功覆盖失败记录。
5. 暂缓：冷启动、3D、额外 bundle 压缩；只有直接影响正确提交或交互响应时纳入当前缺陷。

子代理只接受一个可独立完成的有限问题。默认 Terra medium 用于当前几何定位，简单整理直接完成；没有证据表明需要时不升级模型或并发测试。当前检查点不报告无法取得的实际 token 成本占比。

### WMS 通用候选修复补充（待整轮验收）

已定位成组修复的一个根因：压平同伴阶梯会恢复净距不足的旧通道；整段平移又会改变远处拓扑。新增候选只平移发生交叉的内部线段，按现有最短内部线段长度选择相邻通道，保留其余路径；与原候选共用 64 次生成预算及全部原子校验，并禁止修改本次修复范围之外的同伴线。生产代码不包含图或边 ID 特判。

本轮真实 Worker 的 WMS 全流程质量测试通过，WMS 主干端到端回归与四方向合成场景共 11 项通过，物流回归文件 4 项通过。WMS 中间风险从旧值 32.68 降为约 31.237，测试保留 32.68／8 的原风险上限，允许上游已有改善，并继续要求本次事务改善及最终零违规。类型与相关 Lint 通过。生产构建已通过，既有 4 份预编译产物已同步，只有源码身份变化，未新增缓存图；整轮 CI 正在运行，当前构建浏览器矩阵仍待完成，不能以这些局部结果宣布专项验收结束。日志为 `tmp/wms-peer-relocation-production.log`、`tmp/peer-relocation-regressions.log`、`tmp/peer-relocation-build.log`、`tmp/peer-relocation-precompiled.log` 与 `tmp/peer-relocation-verify.log`。

整轮结果补充：`verify` 静态阶段通过，CI 为 48/49 分片通过，唯一失败是 WMS 冷态测试最后的旧几何指纹；该测试在指纹之前的全部质量、确定性工作量和 25 秒耗时门禁均已通过。保留这些断言和关键路径断言，只将精确指纹同步为本批几何后，原分片 3 文件 / 12 测试全部通过（`tmp/peer-relocation-cold-recheck.log`）。原整轮退出码仍为 1，不将它改记为整轮一次通过。限定 Terra high 只读审查未发现当前差异中可行动的问题，未代替实际测试。TS6 与当前构建浏览器验收仍需完成。

本批本地提交为 `742d775b`，未推送。TS6、指纹测试定向 Lint 通过；当前生产构建的默认 `verify:display-routing-browser` 通过（`tmp/peer-relocation-browser.log`）：最终 SVG 节点穿越和净距风险均为 0，三个物流节点拖拽商业净距无退化，fit/50/100/200% 标签检查、主题和导出检查通过。该入口不能代替四代表图所有布局方向、企业架构图或任意图通用性的证明；这些仍按迭代计划继续验证。

### 当前运行时版本的泳道矩阵（742d775b）

当前生产构建四代表图（物流、WMS 流程、需求分配、TMS）分别连续执行 TB→BT→LR→RL，16/16 通过。每步均执行实际 DOM 同组泳道等尺寸、菜单选中项、业务链语义、最终 SVG 与节点几何一致性检查；节点穿越、最小净距和商业净距风险均为 0，最大输入至路由提交耗时 16692ms，未提高原 30000ms 门禁。WMS 自动选择 compact，其余三图选择 global；紧凑模式允许跨域回移而仍验证域内先后，不能宣称全部图都保持全局单调。

数据索引：`implementation/current-lanes-summary.json`，原始结果为 `implementation/current-lanes-<presetId>.log`（会话证据目录）。这些结果补齐当前版本的四方向实际泳道尺寸验证；尚不代替跨引擎完整矩阵、全部标签视觉检查或企业架构图的剩余通用路由问题。
