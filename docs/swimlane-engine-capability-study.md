# 泳道引擎能力对照：停止无收益的替换

日期：2026-09-08。基准：`6e8bae1f`，本地 elkjs 0.12.0。本文记录实验，不代表新增生产能力。

## 行业能力与项目约束

[ELK partitioning](https://eclipse.dev/elk/reference/options/org-eclipse-elk-partitioning-activate.html)在流程轴上约束分区先后，不能直接用泳道编号代替阶段编号。[半交互排序](https://eclipse.dev/elk/reference/options/org-eclipse-elk-layered-crossingMinimization-semiInteractive.html)保留层内次序；它不承诺不同层的同一泳道具有一致且互不重叠的边界。[分组排序](https://eclipse.dev/elk/reference/options/org-eclipse-elk-layered-considerModelOrder-groupModelOrder-cmGroupOrderStrategy.html)也需要与节点坐标约束区分。

Vizly 已有全局排名、泳道坐标分配、层内重心排序和最终路由验收。不能把“尚未传某个 ELK 参数”直接判断成当前空白的根因。

## 匿名反例

固定六个节点：a/d 属于泳道 0，b/e 属于泳道 1，c/f 属于泳道 2。尺寸依次为 80×40、80×120、120×60、100×80、80×40、160×40。边为 a→f、b→e、c→d，要求全部向前且泳道依次排列。节点间距 48，层间距 80，随机种子 1，关闭独立分量分离；分别运行 RIGHT、DOWN。

| 配置 | 流程全部向前 | 全部层共享有序、互不重叠的泳道边界 |
| --- | --- | --- |
| 默认 layered | 两方向均通过 | 两方向均不满足 |
| partitioning，以泳道编号为 partition | 两方向均不满足 | 两方向均不满足 |
| 分组排序 ENFORCED，默认组 ID 1/2/6，NODES_AND_EDGES，forceNodeModelOrder | 两方向均通过 | 两方向均不满足 |
| semiInteractive，并传入位置 | 两方向均通过 | 两方向均不满足 |

显式把 `cmEnforcedGroupOrders` 作为字符串列表传入本地 JS 引擎时出现属性实例化异常。这是本次序列化形式的失败，不证明 Java ELK 或所有传参形式都不支持该能力。未尝试通过捕获并忽略错误接入生产。

结论只适用于这些明确配置和反例；不能据此声称 ELK 无法实现泳道。它说明应先补齐分区坐标契约，不能把层内排序当作完整泳道支持。

## 业务几何的限定替换实验

固定需求图已测量的节点尺寸、泳道归属和实际边。只用原生 ELK 替换全局阶段坐标，保留现有泳道坐标约束与阶段对齐；显式主流程边赋予方向优先级。两种结果都经完整 Worker 路由和现有质量度量。

| 方向/排名来源 | 总宽 | 总高 | 线长 | 拐点 | 交叉 | 主流程倒向 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| TB / 当前 Dagre | 3461 | 2568 | 10492.5 | 17 | 0 | 0 |
| TB / ELK | 3461 | 2565 | 10432.5 | 17 | 0 | 0 |
| LR / 当前 Dagre | 4810 | 2760 | 12052.5 | 16 | 0 | 0 |
| LR / ELK | 4812 | 2760 | 12012.8 | 16 | 0 | 0 |

四项硬质量均通过，但竖向高度仅减少约 0.12%，横向宽度略增。没有足够可见收益支持增加引擎调用和新的候选分支，因此停止推广。未扩展 WMS 或参数网格，也未将实验脚本加入生产路径。

本地复现记录：`tmp/elk-lane-capability.mjs/json/log`、`tmp/elk-ranked-lane-experiment.ts`、`tmp/elk-ranked-lane.test.mjs`、`tmp/elk-ranked-lane-results.json`、`tmp/elk-ranked-lane.log`。临时脚本不是 CI 门禁，实验通过不能代替产品验收。

## 下一项必须回答的问题

### 补充：定向层扫描对照（基准 fae910ae）

对照当前四轮全邻接重心排序，与四轮按阶段前后交替扫描、只参考本轮上游或下游邻居的排序。保持阶段来源、泳道归属、间距、节点尺寸及全局对齐选项不变，不改依赖语义。固定需求图和 WMS 的已测量输入，各运行 TB/LR 两方向，所有结果经过完整 Worker 路由。

- 需求图 TB 两者均为 3461×2568、总线长 10492.5、17 个拐点、零交叉；LR 两者均为 4810×2760、总线长 12052.5、16 个拐点、零交叉，硬质量通过。该对照没有提供支持替换现有排序的收益。
- WMS 两种排序的全局对齐候选在两个方向均未通过硬质量，完整质量指标返回 unavailable（不是零）。它们都观测到两条主流程倒向；这只是诊断事实，尚不能据此判断硬质量失败的具体原因。
- 本实验强制生成全局对齐候选；它不是产品当前经过候选选择后的 WMS 结果，不代表上线布局回归。没有修改产品代码、候选选择或门禁。
- 停止推广这一排序变体，不扩展参数搜索。下一步仍需明确阶段语义，并从失败候选的具体约束定位问题；不能仅凭“采用了行业算法步骤”认定整体质量改善。

复现生成器：`tmp/create-directional-lane-experiment.mjs`；临时模块及测试：`tmp/directional-lane-experiment.ts`、`tmp/directional-lane.test.mjs`；结果：`tmp/directional-lane-demand-results.json`、`tmp/directional-lane-wms-results.json`；执行日志：`tmp/directional-lane.log`。测试通过表示八次计算对照完成，不表示全部候选可以发布。临时文件不进入 CI，也不属于生产能力。

### WMS 拒绝原因：先处理节点几何

进一步保留完整硬报告后，TB 候选有 24 次障碍命中、22 条最小净距违规、端点未全部锚定；LR 有 9 次障碍命中、6 条最小净距违规。两者严格未桥接交叉均为零，因此不能将其概括为“交叉太多”。实验最初未传生产入口的 `promoteLockedComputedPath`，补齐该选项后确认 44 条连线候选进入准备结果，两个方向硬报告完全相同；本次拒绝不是这个选项差异造成的。

按父链累加最终节点坐标、使用最终 measured 尺寸检查 39 个可见业务节点，TB 存在 43 对矩形重叠，LR 存在 6 对，最深重叠 73 单位。该几何本身不应发布；下一步先用匿名同阶段分支复现坐标复用与对齐的组合问题，再定位初次横轴压缩、阶段对齐和最终坐标分配的责任。尚未证明具体函数根因，不增加路由修复次数来掩盖节点重叠。

证据：`tmp/wms-alignment-diagnostic.log`、`tmp/wms-alignment-promoted.log`、`tmp/alignment-promoted-wms-results.json`、`tmp/wms-alignment-node-audit.mjs/json`。这是强制候选诊断，不是当前产品选中结果的质量声明。

### 根因与修复验收

匿名单泳道、一对多、全部初始坐标为零的反例在四个方向均重叠。全局阶段对齐取消错开的通道后，原横向槽位不再可靠；现在仅在新阶段下原槽位确实重叠时，使用同一次分层计算的横向槽位。无条件替换曾使需求图 LR 从既有 0.2241 适配缩放退回 0.2014，并暴露保存恢复几何差异；该版本未提交。条件替换恢复原有排列，两项实际浏览器问题均消失。

第二处根因由阶段快照确认：WMS 三个分支在最终坐标分配前分别位于 x=64/394/726，之后被共用通道调整压到相同右边界。通道策略把同侧不同节点当成可以复用一列，未检查它们是否处于同一流程带。现在暂存整组位移，检查所有受影响矩形与当前同桶节点的净距，再原子应用；不适合共用通道时保留此前的无重叠位置。仍保留原 256 节点 / 1024 边的通道处理边界。

修复已补入现有语义泳道及最终坐标分配测试文件，覆盖四方向、不同尺寸、输入不变、同阶段共用通道拒绝与原本安全的分行通道保留。临时恢复无条件通道应用后，新增两项回归失败，原十二项通过，证明旧覆盖缺口。

真实页面的需求图及 WMS 各 TB/LR 均通过最终 Worker/DOM 位置尺寸一致性、严格 SVG 几何检查及保存重载。需求图保持既有 0.2809/0.2241 适配缩放，WMS 保持 0.2979/0.1036；因此本批不能宣称这两张图的选中结果有新的可见提升。它修复了通用候选正确性，复杂图整体质量仍须继续推进。浏览器记录：`tmp/lane-clearance-retain-browser.log`，反事实回归：`tmp/lane-channel-mutation.log`。

最终条件替换版本的 WMS 强制对齐候选，两方向 Worker 硬报告均通过，39 个业务节点的矩形重叠均为零，最小矩形轴向分离分别为 64/96 单位。中间无条件替换版本的 LR 连线重叠失败不应继续描述为最终版本状态。最新记录：`tmp/aligned-clearance-final-candidate.log`、`tmp/alignment-promoted-wms-results.json`、`tmp/wms-alignment-node-audit.json`；临时结果文件是最新运行快照，历史失败另由本文与对应日志保留。

下一项应先明确阶段约束：显式主流程、数据关联、反馈和未知类型的边，哪些必须决定阶段先后，哪些仅影响路由。当前公共 Dagre 排名对所有连接赋予层级跨度；仅改排名引擎没有解决这个建模问题。不能直接删除非主流程依赖来获得更紧凑的截图。

先用匿名跨泳道合流、反馈及未知边类型夹具固定该契约，再做一个候选与当前结果的对照。必须保留边、泳道顺序和所有声明的先后约束；未知语义继续沿用既有行为。若同视口仍没有可见改善，则不接入产品。更大规模、嵌套、增量稳定性和性能尾延迟仍按原迭代计划验收，未因本研究而关闭。
